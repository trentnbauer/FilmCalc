diff a/index.html b/index.html	(rejected hunks)
@@ -58,7 +58,7 @@
                 <div class="bg-indigo-50 dark:bg-indigo-900/20 rounded-lg border border-indigo-100 dark:border-indigo-800/30 p-4 text-left">
                     <label for="devSpeed" class="block text-xs font-semibold text-indigo-700 dark:text-indigo-400 uppercase tracking-wide mb-1">Target Speed (ISO)</label>
                     <input type="number" id="devSpeed" class="w-full border p-2 rounded dark:bg-gray-700 dark:border-gray-600">
-                    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Start here — enter the ISO you're shooting at. If it differs from a film's Box Speed below, the cheapest eligible film for that push/pull loads automatically.</p>
+                    <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Start here — enter the ISO you're shooting at. If it differs from a film's Box Speed below, the cheapest saved film loads automatically.</p>
                     <div id="pushPullWarning" class="mt-1"></div>
                     <div id="expiredStackWarning" class="mt-1"></div>
                 </div>
@@ -189,11 +189,6 @@
                         <label for="libraryBoxSpeed" class="block text-xs text-gray-500 dark:text-gray-400 mb-1">Box Speed (ISO)</label>
                         <input type="number" id="libraryBoxSpeed" class="w-full border p-2 rounded dark:bg-gray-700 dark:border-gray-600">
                     </div>
-                    <div>
-                        <label for="libraryMaxPushPull" class="block text-xs text-gray-500 dark:text-gray-400 mb-1">Max Push/Pull (stops)</label>
-                        <input type="number" id="libraryMaxPushPull" min="0" step="0.5" value="1" class="w-full border p-2 rounded dark:bg-gray-700 dark:border-gray-600">
-                        <p class="text-xs text-gray-400 dark:text-gray-500 mt-1">Applies to the whole film stock, not individual bundles. A warning shows in the Calculator if a lab comparison would push/pull this film further than its limit.</p>
-                    </div>
 
                     <label class="flex items-center gap-2 text-sm text-gray-700 dark:text-gray-300">
                         <input type="checkbox" id="filmHiddenCheckbox" class="rounded border-gray-300 dark:border-gray-600 text-blue-600 focus:ring-blue-500">
@@ -962,23 +957,6 @@
             return `<button type="button" data-pin-id="${id}" class="ml-2 text-indigo-400 hover:text-indigo-600 text-xs align-middle" aria-label="Pin for comparison" title="Pin for comparison">📌</button>`;
         }
 
-        // Warns if the Box Speed → Dev Speed gap exceeds the currently-loaded
-        // film's Max Push/Pull limit (set per film stock in the Film Library).
-        // Only shows when a film has actually been loaded via quick lookup —
-        // there's no limit to check against otherwise.
-        function renderPushPullWarning(stops) {
-            const el = document.getElementById('pushPullWarning');
-            if (!el) return;
-            if (loadedFilmMaxPushPull === null || stops <= loadedFilmMaxPushPull) {
-                el.innerHTML = '';
-                return;
-            }
-            el.innerHTML = `<div class="flex items-start gap-2 bg-orange-100 dark:bg-orange-900/30 border border-orange-300 dark:border-orange-700 rounded-lg px-3 py-2">
-                <span class="text-orange-500 dark:text-orange-400">⚠️</span>
-                <p class="text-sm text-orange-800 dark:text-orange-300 font-medium">This pushes/pulls ${stops} stop${stops === 1 ? '' : 's'}, beyond this film's max of ${loadedFilmMaxPushPull} stop${loadedFilmMaxPushPull === 1 ? '' : 's'}</p>
-            </div>`;
-        }
-
         // Shows the camera setting (shooting ISO) and push/pull for this roll at
         // the top of "Labs For This Roll", so it's clear at a glance what the
         // comparison below is actually costing out.
@@ -1315,29 +1311,20 @@
         // Recommends the cheapest film to shoot at a given EI when only Dev Speed
         // is filled in — considers every saved film regardless of native box
         // speed, since the purchase cost doesn't change based on what EI you
-        // rate it at. Native-speed matches (no push/pull needed) are flagged
-        // separately from ones that would need pushing or pulling. The cheapest
-        // film that doesn't exceed its own Max Push/Pull limit is auto-loaded
-        // into the calculator so Labs For This Roll populates without needing
-        // to expand Quick Calculate or click anything.
+        // rate it at. Always loads the single cheapest match into the
+        // calculator so Labs For This Roll populates without needing to
+        // expand Quick Calculate or click anything.
         function renderDevSpeedRecommendation(devSpeed, container) {
             const allFilms = { ...defaultFilms, ...JSON.parse(localStorage.getItem('filmProfiles') || '{}') };
 
-            let idCounter = 0;
             const matches = Object.values(allFilms)
                 .filter(f => !f.hidden)
                 .flatMap(f => {
                     const filmBoxSpeed = parseInt(f.boxSpeed) || 0;
                     if (!filmBoxSpeed) return [];
-                    const stops = Math.round(Math.log2(devSpeed / filmBoxSpeed) * 10) / 10;
-                    const maxPushPull = parseFloat(f.maxPushPull) || 1;
                     return normalizeFilmBundles(f).map(b => ({
-                        id: idCounter++,
                         name: f.name,
                         boxSpeed: filmBoxSpeed,
-                        maxPushPull,
-                        eligible: Math.abs(stops) <= maxPushPull,
-                        stops,
                         rolls: b.rolls,
                         exposures: b.exposures,
                         filmCost: b.filmCost,
@@ -1353,54 +1340,15 @@
                 return;
             }
 
-            function applyMatch(m) {
-                document.getElementById('boxSpeed').value = m.boxSpeed;
-                document.getElementById('filmCost').value = m.filmCost || '';
-                document.getElementById('rolls').value = m.rolls || '1';
-                document.getElementById('exposures').value = m.exposures || '36';
-                devSpeedOverridden = true;
-                loadedFilmMaxPushPull = m.maxPushPull;
-                updateCalcBuyLinkInfo({ buyLink: m.buyLink, storeName: m.storeName });
-                updateLabComparison();
-                updateFilmSpeedComparison();
-            }
-
-            const cheapestEligible = matches.find(m => m.eligible);
-            if (cheapestEligible) {
-                applyMatch(cheapestEligible);
-                return;
-            }
-
-            // Nothing qualifies within its own push/pull limit — show the list
-            // for reference (all marked as exceeding their limit) instead of
-            // silently picking something that shouldn't be pushed/pulled that far.
-            function renderBuyLink(m) {
-                const safeUrl = sanitizeUrl(m.buyLink);
-                if (!safeUrl) return '';
-                const label = m.storeName ? `🛒 Buy from ${escapeHtml(m.storeName)}` : '🛒 Buy';
-                return ` <a href="${escapeHtml(safeUrl)}" target="_blank" rel="noopener noreferrer" class="text-xs font-semibold text-blue-600 dark:text-blue-400 hover:underline">${label}</a>`;
-            }
-
-            function stopsLabel(stops) {
-                if (stops === 0) return `<span class="text-xs text-green-600 dark:text-green-400 font-semibold">Native</span>`;
-                const dir = stops > 0 ? 'Push' : 'Pull';
-                return `<span class="text-xs text-amber-600 dark:text-amber-400">${dir} ${Math.abs(stops)} stop${Math.abs(stops) === 1 ? '' : 's'}</span>`;
-            }
-
-            const rows = matches.slice(0, 6).map((m) => {
-                return `<div class="flex justify-between items-center px-2 py-1 rounded text-xs mb-1 bg-white/60 dark:bg-gray-800/40 opacity-70">
-                    <span class="text-gray-600 dark:text-gray-300">${escapeHtml(m.name)} <span class="opacity-70">(${m.boxSpeed} ISO native, max ${m.maxPushPull} stop${m.maxPushPull === 1 ? '' : 's'})</span> ${stopsLabel(m.stops)}${renderBuyLink(m)}</span>
-                    <span class="font-mono text-gray-500 dark:text-gray-400">${CUR()}${m.costPerPhoto.toFixed(2)}/photo</span>
-                </div>`;
-            }).join('');
-
-            container.innerHTML = `<div class="mt-2 text-left">
-                <div class="flex items-start gap-2 bg-orange-100 dark:bg-orange-900/30 border border-orange-300 dark:border-orange-700 rounded-lg px-3 py-2 mb-2">
-                    <span class="text-orange-500 dark:text-orange-400">⚠️</span>
-                    <p class="text-sm text-orange-800 dark:text-orange-300 font-medium">No saved film can be shot at ${devSpeed} ISO without exceeding its own Max Push/Pull limit</p>
-                </div>
-                ${rows}
-            </div>`;
+            const cheapest = matches[0];
+            document.getElementById('boxSpeed').value = cheapest.boxSpeed;
+            document.getElementById('filmCost').value = cheapest.filmCost || '';
+            document.getElementById('rolls').value = cheapest.rolls || '1';
+            document.getElementById('exposures').value = cheapest.exposures || '36';
+            devSpeedOverridden = true;
+            updateCalcBuyLinkInfo({ buyLink: cheapest.buyLink, storeName: cheapest.storeName });
+            updateLabComparison();
+            updateFilmSpeedComparison();
         }
 
         // ---------- Film Library (cheapest per ISO) ----------
@@ -1493,10 +1441,6 @@
 
         // Dev Speed defaults to Box Speed unless the user has explicitly typed a different value
         let devSpeedOverridden = false;
-        // Tracks the max push/pull limit of whichever film was last loaded via
-        // the Calculator's quick lookup, so Labs For This Roll can warn if the
-        // Dev Speed entered would push/pull it further than that.
-        let loadedFilmMaxPushPull = null;
         const boxSpeedInput = document.getElementById('boxSpeed');
         const devSpeedInput = document.getElementById('devSpeed');
         devSpeedInput.addEventListener('input', () => { devSpeedOverridden = true; });
@@ -1569,7 +1513,6 @@
             const name = e.target.value;
             if (!name) {
                 updateCalcBuyLinkInfo(null);
-                loadedFilmMaxPushPull = null;
                 updateLabComparison();
                 return;
             }
@@ -1589,7 +1532,6 @@
             document.getElementById('filmCost').value = bundle.filmCost || '';
             document.getElementById('rolls').value = bundle.rolls || '1';
             document.getElementById('exposures').value = bundle.exposures || '36';
-            loadedFilmMaxPushPull = parseFloat(film.maxPushPull) || 1;
             updateCalcBuyLinkInfo(bundle);
             updateLabComparison();
             updateFilmSpeedComparison();
@@ -1602,14 +1544,12 @@
             if (!film) {
                 document.getElementById('libraryFilmName').value = '';
                 document.getElementById('libraryBoxSpeed').value = '';
-                document.getElementById('libraryMaxPushPull').value = '1';
                 document.getElementById('filmHiddenCheckbox').checked = false;
                 resetBundles([{}]);
                 return;
             }
             document.getElementById('libraryFilmName').value = film.name;
             document.getElementById('libraryBoxSpeed').value = film.boxSpeed || '';
-            document.getElementById('libraryMaxPushPull').value = film.maxPushPull ?? 1;
             document.getElementById('filmHiddenCheckbox').checked = !!film.hidden;
             resetBundles(normalizeFilmBundles(film));
         });
@@ -1648,7 +1588,6 @@
             document.getElementById('rolls').value = '';
             document.getElementById('exposures').value = '';
             document.getElementById('savedFilmsDropdown').value = '';
-            loadedFilmMaxPushPull = null;
             updateCalcBuyLinkInfo(null);
             updateLabComparison();
             updateFilmSpeedComparison();
@@ -1678,7 +1617,6 @@
             document.getElementById('libraryFilmsDropdown').value = '';
             document.getElementById('libraryFilmName').value = '';
             document.getElementById('libraryBoxSpeed').value = boxSpeed || '';
-            document.getElementById('libraryMaxPushPull').value = '1';
             resetBundles([{ rolls, exposures, filmCost, storeName: '', buyLink: '' }]);
             document.getElementById('libraryFilmName').focus();
         });
@@ -1687,7 +1625,6 @@
             const name = document.getElementById('libraryFilmName').value.trim();
             if (!name) return alert('Enter Film Name');
             const boxSpeed = document.getElementById('libraryBoxSpeed').value;
-            const maxPushPull = parseFloat(document.getElementById('libraryMaxPushPull').value) || 1;
             const bundleBlocks = document.querySelectorAll('#filmBundlesContainer .bundle-block');
             if (bundleBlocks.length === 0) return alert('Add at least one bundle');
             const bundles = Array.from(bundleBlocks).map(block => ({
@@ -1698,7 +1635,7 @@
                 buyLink: block.querySelector('.bundle-buyLink').value.trim()
             }));
             let saved = JSON.parse(localStorage.getItem('filmProfiles') || '{}');
-            saved[name] = { name, boxSpeed, maxPushPull, hidden: document.getElementById('filmHiddenCheckbox').checked, bundles };
+            saved[name] = { name, boxSpeed, hidden: document.getElementById('filmHiddenCheckbox').checked, bundles };
             localStorage.setItem('filmProfiles', JSON.stringify(saved));
             updateFilmDropdowns();
             document.getElementById('libraryFilmsDropdown').value = name;
@@ -1768,7 +1705,6 @@
             return Object.values(all).map(f => ({
                 name: f.name,
                 boxSpeed: Number(f.boxSpeed) || 0,
-                maxPushPull: Number(f.maxPushPull ?? 1),
                 hidden: !!f.hidden,
                 bundles: normalizeFilmBundles(f)
             }));
@@ -1940,7 +1876,7 @@
                     if (result[f.name]) {
                         result[f.name].bundles.push(bundle);
                     } else {
-                        result[f.name] = { name: f.name, boxSpeed: f.boxSpeed, maxPushPull: f.maxPushPull ?? 1, bundles: [bundle] };
+                        result[f.name] = { name: f.name, boxSpeed: f.boxSpeed, bundles: [bundle] };
                     }
                 });
             }
